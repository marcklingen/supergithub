import React from 'react';

// This component is no longer used but we're keeping the file for now
// in case we need to restore it later
const NavigationLinks: React.FC = () => {
  return null;
};

export default NavigationLinks;
